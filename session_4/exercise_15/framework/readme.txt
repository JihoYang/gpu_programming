<<Exercise 15>>

How to execute:

./main <<directory of image>>

Solutions :

Both shared memory and global memory versions are implemented. To test a different memory approach, comment/uncomment corresponding lines on lines 228 - 234.

Shared memory version performs slightly faster than the global memory version, but no significant speedup was observed.

